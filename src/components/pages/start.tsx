import React from 'react';
import MyFirstGrid from '../test/my-first-grids';

// eslint-disable-next-line react/prefer-stateless-function
export default class StartPage extends React.Component {
  render(): React.ReactNode {
    return <MyFirstGrid />;
  }
}
